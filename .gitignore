# Runtime artifacts — generated by the BDLB orchestrator at run time.
# Not part of the system definition; safe to regenerate.
runs/

# Build-time scratch — only meaningful during the build that produced this repo.
existing_specs_summary.json

# Quarantine — should never appear, but if a sub-agent ever isolates a file here, don't publish it.
quarantine/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# Python (in case anyone runs the assembler / renderer here)
__pycache__/
*.pyc
.venv/
venv/
